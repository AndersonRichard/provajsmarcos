const soma = function sum(a, b){
    let resultado = a + b;
    return console.log(`O resultado da soma usando a função expressão é: ${resultado}`);
}

soma(10, 10);


const subtrair = (a, b) => {
    let resultado1 = a - b;
    console.log(`O resultado da subtração usando arrow functions é: ${resultado1}`);
}

subtrair(50,5);